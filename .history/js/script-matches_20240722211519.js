"use strict";

let match = document.querySelectorAll(".match");
console.log(match);

forEach(match => function() {) {

    match.addEventListener("click", function () {
        match.classList.add("hidden");
    });
};
